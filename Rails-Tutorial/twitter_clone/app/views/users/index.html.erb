<% provide(:title, 'All users') %>
<h1>All users</h1>

<!-- adds the paginate links -->
<%= will_paginate %>

<ul class="users">
  <%# @users.each do |user| %>
    <!-- rails automatically looks for _user.html.erb -->
    <%# render user %>
  <%# end %>

  <!-- rails automatically iterates through @users, rendering each user with _user.html.erb -->
  <%= render @users %>
</ul>

<%= will_paginate %>
